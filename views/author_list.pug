extends layout

block content
    h1=title

    ui
        each author in author_list
            li
                a(href=author.url) #{author.name}
                | &nbsp &nbsp
                | (#{author.livedYears})

        else
            li
                | There are no authors